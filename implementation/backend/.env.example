# Default: local MongoDB on port 27017 (matches application.yml).
MONGODB_URI=mongodb://localhost:27017/ledger

# Optional overrides
# DATABASE=mongo
# BASE_URL=http://localhost:8080/v1
# DEMO_USER=benchmark@demo

# Atlas example (only if not using local MongoDB):
# MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/ledger
