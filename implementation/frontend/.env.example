# Frontend server
PORT=3000
SESSION_SECRET=change-me-in-production

# Backend ledger API (Spring Boot)
API_URL=http://localhost:8080/v1

# Serve fixture data when backend endpoints are unavailable (development only)
USE_FIXTURES=false
